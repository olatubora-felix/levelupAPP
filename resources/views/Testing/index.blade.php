<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Testing Code</title>
</head>
<body>
  <h1>Testing The Code</h1>
  <ul>
    {{-- @foreach ($mydownline as $downline)
    <li><a href="{{ route('show',$downline->id) }}">{{ $downline->name }}</a></li> 
    @endforeach --}}
    {{-- {{ $mydownline }} --}}
      {{-- @foreach ($allUsers as $user) --}}
        {{-- {{ $user->name }} --}}
        {{-- {{ $user[$key]['name']  }} --}}
        {{-- <li><a href="{{ route('show', $user->ref_id) }}">{{ $user->name }}</a></li>   --}}
      {{-- @endforeach --}}
      
  </ul>
</body>
</html>